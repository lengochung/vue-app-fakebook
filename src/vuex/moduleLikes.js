import DB from "../APIs"

const moduleLikes = {
    state: {},
    mutations: {
        // like(context, uid, pid) {
        //     DB.load("likes").insert(uid, pid)
        // },
        // dislike(context, uid, pid) {
        //     DB.load("likes").deleteDlWhere("uid", uid, "pid", pid)
        // }
    },
    getters: {},
    actions: {
        
    }
}

export default moduleLikes